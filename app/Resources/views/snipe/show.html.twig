{% extends 'base.html.twig' %}

{% block body %}
    <h1>Snipe</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ snipe.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('snipe_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('snipe_edit', { 'id': snipe.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
